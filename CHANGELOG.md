# Changelog

## 2026-07-26
- Fix: flipped the start/finish line sign orientation in `packages/render/src/world.ts` so the sign faces the correct direction.
- Fix: corrected the overhead `START / FINISH` banner orientation in `packages/render/src/landmarks.ts` so the text reads forwards.
- Add: created a project changelog file for future updates.
